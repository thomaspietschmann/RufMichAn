<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Exact alarms: API 31-32 (user-revocable), API 33+ auto-granted via USE_EXACT_ALARM -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Show full-screen intent (incoming call overlay), required explicitly on API 34+ -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Post notifications (runtime permission on API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Reschedule alarms after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Ringtone vibration -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Wake lock for CPU at alarm fire time + proximity sensor in-call -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Foreground service for the incoming-call presentation layer -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Optional: ask the user to exempt this app from battery optimisation -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".RufMichAnApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RufMichAn">

        <!-- Main UI entry point -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Incoming-call overlay: shown over the lock screen like a real call -->
        <activity
            android:name=".call.CallActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.RufMichAn.Call" />

        <!-- Foreground service: owns the notification, ringtone and vibration lifecycle -->
        <service
            android:name=".call.CallService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <!-- Required for Google Play; F-Droid does not validate this -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Simulates incoming calls from user-defined contacts" />
        </service>

        <!-- Woken up by AlarmManager when a scheduled call is due -->
        <receiver
            android:name=".alarm.AlarmReceiver"
            android:exported="false" />

        <!-- Re-schedules alarms after a device reboot -->
        <receiver
            android:name=".alarm.BootReceiver"
            android:exported="true">
            <!-- S2: Only BOOT_COMPLETED — app is not Direct-Boot-aware (Room is CE-encrypted). -->
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
