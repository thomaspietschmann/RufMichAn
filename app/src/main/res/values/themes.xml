<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.RufMichAn" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Full-screen theme for CallActivity: no action bar, dark status bar -->
    <style name="Theme.RufMichAn.Call" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="o_mr1">shortEdges</item>
    </style>
</resources>
